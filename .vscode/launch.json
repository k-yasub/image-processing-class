{
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "Launch",
            "program": "${workspaceFolder}/classtest.exe",
            "args": [],
            "cwd": "${workspaceFolder}"
        }
    ]
}